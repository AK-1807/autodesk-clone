'use client'
import React from 'react'

function  ShowList() {
    
    
  return (
    <div>
      hi
          {/* {todos.map((item) => {
          return (<p>{item.id}</p>)
      })} */}
    </div>
  )
}

export default ShowList